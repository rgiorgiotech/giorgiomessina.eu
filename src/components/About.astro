---
import SectionTitle from './SectionTitle.astro';
import { education } from '../data/education';
import { Drum, Code2, MapPin, GraduationCap } from 'lucide-react';
---

<section id="about" class="py-16 bg-slate-50/50 scroll-mt-16">
  <SectionTitle title="About me & Education" />

  <div class="grid md:grid-cols-5 gap-12 items-start">
    
    <div class="md:col-span-2 space-y-6">
      <div class="prose text-slate-600 leading-relaxed">
        <h3 class="font-semibold text-slate-800 flex items-center gap-2">🎓 Academic Background</h3>
        <p class="mt-1">
          Master’s student with a Bachelor’s in Computer Engineering and solid foundations in telecommunications, networks, programming, and electronics.
        </p>
        <h3 class="font-semibold text-slate-800 flex items-center gap-2 mt-4">💡 Interests & Passions</h3>
        <p class="mt-1">
          Strong interest in fiber-optic networks and radio-satellite and terrestrial communication systems.
          Passionate about radio and TV production, keeping up with audio-video technology developments.
          Experienced in local networks, IT infrastructures, and advanced networking solutions.
        </p>
        <h3 class="font-semibold text-slate-800 flex items-center gap-2 mt-4">🎵 Music & Audio Tech</h3>
        <p class="mt-1">
          Longtime musician with training in music theory and ensemble performance, skilled in drums and piano, experienced in studio recording,
          DAWs, and music production software, and exploring creative sound design with various VSTs.
        </p>
      </div>
    </div>

    <div class="md:col-span-3">
      <div class="relative pl-8 border-l-2 border-orange-100 space-y-10">
        
        {education.map((item) => (
          <div class="relative">
            <span class="absolute -left-[41px] top-1 flex h-6 w-6 items-center justify-center rounded-full bg-orange-100 border-2 border-orange-500 ring-4 ring-white">
              <GraduationCap class="w-3 h-3 text-orange-600" />
            </span>

            <div class="flex flex-col sm:flex-row sm:items-center sm:justify-between mb-2">
              <h3 class="text-xl font-bold text-slate-800">{item.degree}</h3>
              <span class="text-sm font-mono text-orange-600 bg-orange-50 px-2 py-1 rounded mt-1 sm:mt-0 w-fit">
                {item.period}
              </span>
            </div>
            
            <div class="text-lg font-medium text-slate-700 mb-2">{item.school}</div>
            <p class="text-slate-500 text-sm">{item.description}</p>
          </div>
        ))}

      </div>
    </div>

  </div>
</section>
